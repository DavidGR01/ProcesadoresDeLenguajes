let number ent;
let number cont; 
cont = 0;
function number restaHastaN(number n, string texto)
{
	let number resta;
	resta = 0;
	while(cont < 0) {
		let number f;
		f = 5;
		resta = resta - cont -f;
		cont = ++cont;
	}
	alert(texto);
	return resta;
}
let string texto = 'Buenos dias';
let number resta;
let number _N; //No se puede iniciar el nombre de una variable con un _
N = 1000;
resta = restaHastaN(texto,N); // Argumentos erroneos
let string frase;
frase = 'Que pequeño sos boludo';
if(res < -1000)
	alert(frase);